apiVersion: 1

datasources:
  - name: connection-to-aggregatorpg
    type: postgres
    uid: efu0bzagddssgd
    url: ${GRAFANA_POSTGRES_HOST}:5432
    user: ${POSTGRES_USER}
    isDefault: true
    editable: false
    jsonData:
      database: ${POSTGRES_DB}
      sslmode: disable
      postgresVersion: 1700
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
